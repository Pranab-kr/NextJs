
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model post{
  id    Int    @id @default(autoincrement())
  title String
  description String?

  createdAt DateTime @default(now())
}

const page = () => {
  // a route group is a way to organize without affecting the URL structure
  //you xan define a route group by wrapping a set of routes in parentheses ()
  //sharing layout across multiple routes without duplicating code or adding extra segments to the URL
  return <div>SignUp Page</div>;
};

export default page;
